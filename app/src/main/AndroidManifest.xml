<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.card_menu">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".info_view"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".lesson_view"
            android:launchMode="singleTask"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".pdf_view"
            android:launchMode="singleTask"
            android:parentActivityName=".lesson_view" />
        <activity
            android:name=".image_list"
            android:launchMode="singleTask"
            android:parentActivityName=".pdf_view"></activity>
        <activity
            android:name=".image_viewer"
            android:parentActivityName=".image_list" />
    </application>

</manifest>